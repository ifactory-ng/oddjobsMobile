<com.ifactory.oddjobs.FragmentNavigationDrawer xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawerlayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <FrameLayout android:id="@+id/mainContent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />
    <ListView
        android:id="@+id/drawerList"
        android:entries="@array/menu_item"
        android:layout_width="240dp"
        android:layout_gravity="start|bottom"
        android:layout_height="match_parent"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="0dp"
        android:choiceMode="singleChoice"
        android:background="@android:color/background_light"
        />
</com.ifactory.oddjobs.FragmentNavigationDrawer>
